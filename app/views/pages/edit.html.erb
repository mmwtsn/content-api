<%= form_for :page, url: page_path(@page), method: :patch do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, autofocus: true %>

  <%= f.label :body %>
  <%= f.text_area :body %>

  <%= f.submit 'save' %>
<% end %>
