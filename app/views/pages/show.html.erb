<%# image_tag @page.header %>

<%= render 'tmp_ibm_top' %>
<h2 class="section-header">
  <%= @page.title %>
</h2>

<%= render 'tmp_ibm_mid' %>
<p>
  <%= @page.body %>
</p>

<%= render 'tmp_ibm_bot' %>
