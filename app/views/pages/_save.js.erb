<% if object.errors.any? %>
  <%= render 'errors', object: object %>
<% else %>
  $('.solution.new').empty();
  $('#new-page').after('<%= j render("index", object: object) %>').show(); // TODO - These selectors are a mess
<% end %>
